# Production (Vercel): set to your Render Web Service URL — origin only, no path.
# Example: https://e-inter-api.onrender.com
# No trailing slash; no quotes in the Vercel dashboard.
# Leave unset for local dev (Vite proxies /api → http://127.0.0.1:8787).
# VITE_API_ORIGIN=https://your-service.onrender.com
